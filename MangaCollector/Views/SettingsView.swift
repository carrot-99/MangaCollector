//  SettingsView.swift

import SwiftUI

struct SettingsView: View {
    var body: some View {
        VStack {
            Text("設定画面")
            // ここに設定項目を配置します
        }
        // 背景色やテキストカラーの設定
    }
}
